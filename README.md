# gestoAPI
Real-time hand gesture recognition system with hybrid architecture — Python (OpenCV + MediaPipe) detects gestures via webcam and sends UDP commands to a Java server that triggers keyboard shortcuts and REST API calls.
